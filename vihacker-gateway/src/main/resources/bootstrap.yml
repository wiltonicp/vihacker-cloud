spring:
  application:
    name: ViHacker-Gateway
  cloud:
    nacos:
      #      config:
      #        server-addr: :8001
      #        group: TEST_GROUP
      #        prefix: vihacker-auth
      #        file-extension: yaml
      discovery:
        server-addr: vihacker.top:8101
        namespace: 07230e1a-12e6-4209-838f-fa60cf013e7e
        group: TEST_GROUP


logging:
  level:
    org:
      springframework:
        boot:
          actuate:
            endpoint:
              EndpointId: error
    com:
      alibaba:
        cloud:
          nacos:
            client:
              NacosPropertySourceBuilder: error